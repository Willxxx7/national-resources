meta {
  name: M4.3 - Login with invalid password
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/login
  body: formUrlEncoded
  auth: none
}

body:form-urlencoded {
  email: {{adminEmail}}
  password: WrongPassword123
}

tests {
  test("Invalid password rejected", function() {
    expect([200, 302, 401, 422]).to.include(res.status);
  });
}
