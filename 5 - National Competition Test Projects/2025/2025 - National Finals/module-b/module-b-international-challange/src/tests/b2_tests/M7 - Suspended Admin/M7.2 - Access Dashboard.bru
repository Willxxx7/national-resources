meta {
  name: M7.2 - Access dashboard after failed login
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/
  body: none
  auth: none
}

tests {
  test("Suspended admin cannot access dashboard", function() {
    expect([200, 302, 401, 403]).to.include(res.status);
  });
}
