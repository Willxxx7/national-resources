meta {
  name: M9.1 - Access private event with valid code
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/events/private_events/private_test_event?access_code=TestCode123
  body: none
  auth: none
}

tests {
  test("Valid access code grants access", function() {
    expect([200, 302, 404]).to.include(res.status);
  });
}
