meta {
  name: M3.0 - Login as admin
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/login
  body: formUrlEncoded
  auth: none
}

body:form-urlencoded {
  email: {{adminEmail}}
  password: {{adminPassword}}
}

tests {
  test("Admin login", function() {
    expect(res.status).to.be.within(200, 299);
  });
}
